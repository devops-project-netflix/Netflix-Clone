language: python


jobs:
  include:
  # --------------------------------------------
  - stage: test
    skip_cleanup: true
    script: 
      - make test
  # --------------------------------------------
  - stage: build+push:latest

    deploy:
      provider: script
      script: ./travis_scripts/before_deploy.sh
    on:
      branch: sujay_sl5202